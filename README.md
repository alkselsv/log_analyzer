## Запуск приложения

```bash
python main.py --root_dir ./users --models_dir ./models

Options
  -h, --help                show this help message and exit
  --root_dir ROOT_DIR       Path to the root dir
  --models_dir MODELS_DIR   Path to the classification models
```
